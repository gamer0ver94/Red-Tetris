//Maps backend state into frontend state

export function build_render_payload(game, player_id): RenderPayload{}

export function build_opponent_spectrum(board){}

export function build_visible_board(board){}