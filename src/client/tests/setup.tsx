// Temporarily commented out to keep the client build passing.
// The project does not currently have @testing-library/jest-dom available during build.
// Re-enable this import once the dependency is installed and test setup is finalized.
// import "@testing-library/jest-dom";
